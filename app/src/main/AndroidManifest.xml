<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iwxyi.easymeeting">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- usesCleartextTraffic 允许明文传输，不然 Android P 不允许未加密的联网方式 -->
    <application
        android:name=".Globals.App"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Users.LoginActivity" />
        <activity android:name=".Users.RegisterActivity" />
        <activity
            android:name=".Users.PersonActivity"
            android:label="@string/title_activity_person"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AddLeaseActivity"
            android:label="@string/title_activity_add_lease"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SettingsActivitys.SettingsActivity"
            android:label="@string/title_activity_settings"></activity>
    </application>

</manifest>